# 文档相似度分析系统 - 手动测试指南

## 概述

本指南提供了完整的手动测试步骤，用于验证认证 token 缓存问题修复后的功能。

## 前置条件

1. **应用已启动**
   ```bash
   cd /home/ubuntu/doc-similarity-analyzer
   npm run dev
   ```
   
2. **应用可访问**
   - 本地访问: http://localhost:3000
   - 公网访问: https://3000-ivyi6im400mhs1jef6583-52dc4424.sg1.manus.computer

3. **数据库已初始化**
   - 用户表已创建
   - 初始用户已创建 (admin@system.local)

## 测试场景

### 场景 1: 新用户注册和自动登录

#### 目标
验证新用户能够成功注册并自动登录，且页面显示正确的用户信息。

#### 测试步骤

1. **打开登录页面**
   - 导航到 http://localhost:3000/login
   - 应该看到登录表单

2. **切换到注册标签**
   - 点击"注册"标签
   - 应该看到注册表单

3. **填写注册信息**
   - 邮箱: `testuser1@example.com`
   - 用户名: `testuser1`
   - 密码: `password123`

4. **提交注册**
   - 点击"注册"按钮
   - 应该看到加载状态

5. **验证自动登录**
   - 页面应该自动跳转到仪表板
   - 右上角应该显示 "testuser1"
   - 欢迎信息应该说 "欢迎, testuser1"

6. **验证 Token 正确性**
   - 打开浏览器开发者工具 (F12)
   - 进入 Console 标签
   - 执行: `localStorage.getItem('auth_token')`
   - 应该返回一个 JWT token

7. **验证用户信息**
   - 执行: `localStorage.getItem('manus-runtime-user-info')`
   - 应该看到包含 `testuser1` 的用户信息 JSON

#### 预期结果
- ✅ 注册成功
- ✅ 自动登录到仪表板
- ✅ 页面显示正确的用户名 (testuser1)
- ✅ localStorage 中有新的 token
- ✅ 用户信息与登录用户一致

#### 失败排查
如果测试失败，检查以下内容：

1. **页面显示错误的用户名**
   - 原因: tRPC 缓存没有被清除
   - 解决: 检查 Login.tsx 中是否添加了 `utils.auth.me.invalidate()`

2. **localStorage 中没有 token**
   - 原因: token 没有被保存
   - 解决: 检查 Login.tsx 中的 `localStorage.setItem('auth_token', data.token)`

3. **用户信息与登录用户不符**
   - 原因: 缓存中保存的是旧用户信息
   - 解决: 清除浏览器缓存并重新测试

---

### 场景 2: 用户切换（登出后以不同用户登录）

#### 目标
验证用户能够成功登出，并以不同用户身份登录，页面显示新用户信息。

#### 测试步骤

1. **从场景 1 继续**
   - 应该已经以 testuser1 身份登录

2. **打开用户菜单**
   - 点击右上角的用户头像或用户名
   - 应该看到下拉菜单

3. **点击登出**
   - 点击"登出"选项
   - 应该看到加载状态

4. **验证已登出**
   - 页面应该重定向到登录页面
   - localStorage 中的 auth_token 应该被清除
   - 执行: `localStorage.getItem('auth_token')`
   - 应该返回 `null`

5. **以不同用户登录**
   - 邮箱: `admin@system.local`
   - 密码: `admin123` (或实际的 admin 密码)
   - 点击"登录"

6. **验证新用户登录**
   - 页面应该跳转到仪表板
   - 右上角应该显示 "admin"
   - 欢迎信息应该说 "欢迎, admin"

7. **验证 Token 和用户信息**
   - 执行: `localStorage.getItem('auth_token')`
   - 应该返回一个新的 JWT token
   - 执行: `localStorage.getItem('manus-runtime-user-info')`
   - 应该看到包含 "admin" 的用户信息

#### 预期结果
- ✅ 登出成功
- ✅ Token 被清除
- ✅ 重定向到登录页面
- ✅ 新用户登录成功
- ✅ 页面显示新用户名 (admin)
- ✅ localStorage 中有新的 token

#### 失败排查
如果测试失败，检查以下内容：

1. **登出后页面仍显示旧用户**
   - 原因: 登出时缓存没有被清除
   - 解决: 检查 useAuth hook 中的 logout 函数

2. **登录新用户后页面显示旧用户**
   - 原因: 同样的 tRPC 缓存问题
   - 解决: 检查修复是否正确应用

3. **Token 没有被更新**
   - 原因: 登录成功但 token 没有被保存
   - 解决: 检查浏览器控制台是否有错误

---

### 场景 3: 文件上传功能

#### 目标
验证用户能够成功上传文件，并且文件出现在文档列表中。

#### 测试步骤

1. **确保已登录**
   - 应该以某个用户身份登录（例如 testuser1）

2. **进入仪表板**
   - 导航到 http://localhost:3000/dashboard
   - 应该看到"我的文档"列表

3. **点击上传文档**
   - 点击"上传文档"按钮
   - 应该看到上传对话框

4. **选择文件**
   - 点击文件选择区域或"选择文件"按钮
   - 选择一个支持的文件格式:
     - TXT (纯文本)
     - DOCX (Word 文档)
     - PDF (PDF 文档)
     - PPTX (PowerPoint)
     - XLSX (Excel)
     - Markdown
     - HTML

5. **上传文件**
   - 确认文件已选择
   - 点击"上传"按钮
   - 应该看到上传进度

6. **验证上传成功**
   - 等待上传完成
   - 对话框应该关闭
   - 新文件应该出现在"我的文档"列表中
   - 文件名应该正确显示

7. **验证文件属性**
   - 点击文件查看详情
   - 应该看到:
     - 文件名
     - 上传时间
     - 文件大小
     - 文件类型

#### 预期结果
- ✅ 上传对话框打开
- ✅ 文件选择成功
- ✅ 上传完成
- ✅ 文件出现在列表中
- ✅ 文件属性正确

#### 失败排查
如果测试失败，检查以下内容：

1. **上传失败，显示错误信息**
   - 检查浏览器控制台的错误信息
   - 可能的原因:
     - Token 过期
     - 文件大小超过限制 (10MB)
     - 文件格式不支持
     - 服务器错误

2. **文件上传但未出现在列表中**
   - 刷新页面
   - 检查浏览器控制台是否有错误
   - 检查服务器日志

3. **上传时显示认证错误**
   - 原因: Token 无效或过期
   - 解决: 重新登录

---

### 场景 4: 分析任务创建

#### 目标
验证用户能够创建分析任务，并查看分析结果。

#### 测试步骤

1. **确保已上传文件**
   - 应该至少有 2 个文档已上传
   - 如果没有，先执行场景 3

2. **进入批量对比页面**
   - 点击左侧菜单的"批量对比"
   - 应该看到文档列表

3. **选择文档**
   - 选择至少 2 个文档
   - 应该看到"已选择 X 个文档"

4. **开始分析**
   - 点击"开始分析"按钮
   - 应该看到加载状态

5. **等待分析完成**
   - 分析可能需要几秒到几分钟
   - 应该看到进度提示

6. **查看分析结果**
   - 分析完成后应该显示结果
   - 应该看到:
     - 相似度百分比
     - 相似的文本段落
     - 差异分析

7. **验证结果准确性**
   - 检查相似度是否合理
   - 检查相似的文本是否正确

#### 预期结果
- ✅ 文档选择成功
- ✅ 分析任务创建成功
- ✅ 分析完成
- ✅ 结果显示正确
- ✅ 相似度计算准确

#### 失败排查
如果测试失败，检查以下内容：

1. **分析失败**
   - 检查浏览器控制台的错误信息
   - 检查服务器日志
   - 可能的原因:
     - 文件格式不支持
     - 文件内容为空
     - 服务器错误

2. **结果不显示**
   - 刷新页面
   - 检查是否有错误信息
   - 尝试重新分析

---

### 场景 5: 报告导出

#### 目标
验证用户能够以不同格式导出分析报告。

#### 测试步骤

1. **确保有分析结果**
   - 应该已经完成场景 4
   - 应该有分析结果显示

2. **打开导出菜单**
   - 点击结果页面的"导出"按钮
   - 应该看到导出选项菜单

3. **选择导出格式**
   - PDF: 点击"导出为 PDF"
   - Word: 点击"导出为 Word"
   - Excel: 点击"导出为 Excel"

4. **验证导出**
   - 文件应该自动下载
   - 下载文件夹中应该出现新文件
   - 文件名应该包含时间戳或任务 ID

5. **验证文件内容**
   - 打开下载的文件
   - 应该看到:
     - 分析结果
     - 相似度信息
     - 文档名称
     - 生成时间

#### 预期结果
- ✅ 导出菜单打开
- ✅ 文件下载成功
- ✅ 文件格式正确
- ✅ 文件内容完整

#### 失败排查
如果测试失败，检查以下内容：

1. **导出失败**
   - 检查浏览器控制台的错误信息
   - 可能的原因:
     - 文件生成失败
     - 网络错误
     - 浏览器安全限制

2. **文件内容不完整**
   - 检查文件是否正确生成
   - 尝试重新导出

---

## 测试记录表

### 场景 1: 新用户注册和自动登录

| 检查项 | 预期 | 实际 | 状态 | 备注 |
|--------|------|------|------|------|
| 注册页面加载 | 显示注册表单 | | ⏳ | |
| 表单填写 | 接受输入 | | ⏳ | |
| 注册提交 | 显示加载状态 | | ⏳ | |
| 自动登录 | 跳转到仪表板 | | ⏳ | |
| 用户名显示 | 显示 testuser1 | | ⏳ | |
| Token 保存 | localStorage 有 token | | ⏳ | |
| 用户信息 | 与登录用户一致 | | ⏳ | |

### 场景 2: 用户切换

| 检查项 | 预期 | 实际 | 状态 | 备注 |
|--------|------|------|------|------|
| 登出菜单 | 显示登出选项 | | ⏳ | |
| 登出成功 | 重定向到登录页 | | ⏳ | |
| Token 清除 | localStorage 无 token | | ⏳ | |
| 新用户登录 | 登录成功 | | ⏳ | |
| 新用户显示 | 显示 admin | | ⏳ | |
| 新 Token | localStorage 有新 token | | ⏳ | |

### 场景 3: 文件上传

| 检查项 | 预期 | 实际 | 状态 | 备注 |
|--------|------|------|------|------|
| 上传对话框 | 打开成功 | | ⏳ | |
| 文件选择 | 选择成功 | | ⏳ | |
| 上传进度 | 显示进度 | | ⏳ | |
| 上传完成 | 对话框关闭 | | ⏳ | |
| 文件列表 | 显示新文件 | | ⏳ | |
| 文件属性 | 显示正确 | | ⏳ | |

### 场景 4: 分析任务

| 检查项 | 预期 | 实际 | 状态 | 备注 |
|--------|------|------|------|------|
| 文档选择 | 选择成功 | | ⏳ | |
| 分析开始 | 显示加载状态 | | ⏳ | |
| 分析完成 | 显示结果 | | ⏳ | |
| 相似度显示 | 显示百分比 | | ⏳ | |
| 相似文本 | 显示正确 | | ⏳ | |

### 场景 5: 报告导出

| 检查项 | 预期 | 实际 | 状态 | 备注 |
|--------|------|------|------|------|
| 导出菜单 | 打开成功 | | ⏳ | |
| PDF 导出 | 文件下载 | | ⏳ | |
| Word 导出 | 文件下载 | | ⏳ | |
| Excel 导出 | 文件下载 | | ⏳ | |
| 文件内容 | 内容完整 | | ⏳ | |

---

## 常见问题

### Q1: 如何查看 localStorage？
**A**: 打开浏览器开发者工具 (F12)，进入 Application 标签，选择 Storage > Local Storage，选择应用域名。

### Q2: 如何查看 JWT Token 内容？
**A**: 
1. 复制 localStorage 中的 auth_token
2. 访问 https://jwt.io
3. 将 token 粘贴到 Encoded 区域
4. 在 Decoded 区域查看内容

### Q3: 如何重置测试环境？
**A**: 
1. 清除浏览器 localStorage: `localStorage.clear()`
2. 清除浏览器 cookies
3. 刷新页面
4. 重新登录

### Q4: 如何查看服务器日志？
**A**: 
1. 查看应用启动的终端输出
2. 或查看应用日志文件 (如果配置了)

### Q5: 上传文件大小限制是多少？
**A**: 默认限制是 10MB。可以在服务器配置中修改。

---

## 测试完成检查清单

- [ ] 场景 1: 新用户注册和自动登录 - 通过
- [ ] 场景 2: 用户切换 - 通过
- [ ] 场景 3: 文件上传 - 通过
- [ ] 场景 4: 分析任务创建 - 通过
- [ ] 场景 5: 报告导出 - 通过
- [ ] 所有测试记录已填写
- [ ] 没有发现 bug
- [ ] 用户体验良好

---

## 测试总结

测试日期: _______________
测试人员: _______________
总体结果: ☐ 通过 ☐ 部分通过 ☐ 失败

### 发现的问题
1. 
2. 
3. 

### 改进建议
1. 
2. 
3. 

### 签名
测试人员: _________________ 日期: _________
审核人: _________________ 日期: _________
