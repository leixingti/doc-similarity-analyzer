# Railway 部署脚本使用说明

本文档介绍了如何使用提供的 Bash 脚本快速部署应用到 Railway。

## 📋 脚本清单

### 1. quick-deploy.sh (推荐使用)
**用途**: 一键快速部署准备  
**功能**: 生成 JWT_SECRET、验证配置、输出部署步骤  
**使用**: 最简单快速的方式

### 2. railway-deploy.sh
**用途**: 完整的部署前准备  
**功能**: 详细的检查和配置，包括 Git 验证  
**使用**: 需要更详细的验证

### 3. verify-deployment.sh
**用途**: 部署后验证  
**功能**: 检查应用是否正常运行  
**使用**: 部署完成后验证

### 4. .env.railway.example
**用途**: 环境变量模板  
**功能**: 参考环境变量配置  
**使用**: 手动配置时参考

## 🚀 快速开始（3 步）

### 第 1 步：运行快速部署脚本

```bash
cd /home/ubuntu/doc-similarity-analyzer
chmod +x scripts/quick-deploy.sh
./scripts/quick-deploy.sh
```

**脚本会:**
- 生成 JWT_SECRET
- 验证配置文件
- 创建 .env.railway 文件
- 显示部署步骤
- 复制 JWT_SECRET 到剪贴板（如果支持）

### 第 2 步：在 Railway 中部署

按照脚本输出的步骤：
1. 访问 https://railway.app
2. 创建新项目
3. 连接 GitHub 仓库
4. 添加 MySQL 数据库
5. 配置环境变量
6. 开始部署

### 第 3 步：验证部署

部署完成后，运行验证脚本：

```bash
./scripts/verify-deployment.sh https://your-app.railway.app
```

## 📖 详细使用说明

### quick-deploy.sh（推荐）

**最简单的一键部署脚本**

```bash
./scripts/quick-deploy.sh
```

**输出内容:**
- 生成的 JWT_SECRET（自动复制到剪贴板）
- 配置验证结果
- 详细的部署步骤
- 快速参考信息

**优点:**
- 操作简单
- 输出清晰
- 自动复制 JWT_SECRET
- 适合快速部署

**缺点:**
- 检查项较少
- 不验证 Git 状态

---

### railway-deploy.sh（完整）

**完整的部署前准备脚本**

```bash
./scripts/railway-deploy.sh
```

**功能:**
1. 检查前置条件（Node.js、Git）
2. 生成 JWT_SECRET
3. 创建 .env.railway 文件
4. 验证部署配置
5. 验证 Git 状态
6. 显示部署步骤
7. 显示快速参考

**输出内容:**
- 详细的检查结果
- 生成的 JWT_SECRET
- 环境变量文件内容
- 完整的部署步骤
- 故障排除建议

**优点:**
- 检查项完整
- 验证 Git 状态
- 输出详细
- 适合完整部署

**缺点:**
- 输出较多
- 需要更多时间

---

### verify-deployment.sh（验证）

**部署后验证脚本**

```bash
./scripts/verify-deployment.sh https://your-app.railway.app
```

或

```bash
./scripts/verify-deployment.sh your-app.railway.app
```

**检查项:**
1. 应用是否在线
2. 健康检查端点
3. API 端点
4. 响应时间
5. SSL 证书

**输出内容:**
- 各项检查的通过/失败状态
- 响应时间
- SSL 证书信息
- 诊断建议

**使用场景:**
- 部署完成后验证
- 应用出现问题时诊断
- 定期检查应用状态

---

## 🔧 环境变量配置

### 使用脚本生成的 .env.railway 文件

脚本会自动创建 `.env.railway` 文件，包含所有必需的环境变量：

```bash
NODE_ENV=production
JWT_SECRET=<生成的随机字符串>
PORT=3000
LOG_LEVEL=info
DATABASE_URL=mysql://root:password@mysql.railway.internal:3306/railway
STORAGE_PATH=.storage/documents
VITE_APP_TITLE=文档相似度分析系统
```

### 手动配置环境变量

如果需要手动配置，可以参考 `.env.railway.example` 文件：

```bash
cat scripts/.env.railway.example
```

然后在 Railway 控制面板中手动输入环境变量。

---

## 📝 脚本工作流程

### quick-deploy.sh 流程

```
┌─────────────────────────────────────────┐
│ 开始                                     │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 生成 JWT_SECRET                         │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 验证配置文件                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 创建 .env.railway 文件                  │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 显示部署步骤                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 复制 JWT_SECRET 到剪贴板                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 完成                                     │
└─────────────────────────────────────────┘
```

### railway-deploy.sh 流程

```
┌─────────────────────────────────────────┐
│ 开始                                     │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 检查前置条件                             │
│ (Node.js, Git)                          │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 生成 JWT_SECRET                         │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 创建 .env.railway 文件                  │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 验证部署配置                             │
│ (railway.json, Dockerfile, etc)         │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 验证 Git 状态                            │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 显示部署步骤                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 显示快速参考                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 完成                                     │
└─────────────────────────────────────────┘
```

### verify-deployment.sh 流程

```
┌─────────────────────────────────────────┐
│ 开始                                     │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 检查应用是否在线                         │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 检查健康检查端点                         │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 检查 API 端点                            │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 检查响应时间                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 检查 SSL 证书                            │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 生成验证报告                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 显示验证结果                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ 完成                                     │
└─────────────────────────────────────────┘
```

---

## 🎯 常见使用场景

### 场景 1：快速部署（推荐）

```bash
# 1. 运行快速部署脚本
./scripts/quick-deploy.sh

# 2. 复制输出的 JWT_SECRET
# 3. 在 Railway 中创建项目
# 4. 配置环境变量
# 5. 开始部署
```

### 场景 2：完整部署

```bash
# 1. 运行完整部署脚本
./scripts/railway-deploy.sh

# 2. 查看详细的检查结果
# 3. 按照步骤在 Railway 中部署
# 4. 验证部署
```

### 场景 3：验证已部署的应用

```bash
# 部署完成后，验证应用
./scripts/verify-deployment.sh https://your-app.railway.app

# 查看验证结果
# 如果有问题，查看建议
```

### 场景 4：定期检查应用状态

```bash
# 定期运行验证脚本
./scripts/verify-deployment.sh https://your-app.railway.app

# 监控应用健康状态
```

---

## 🐛 故障排除

### 脚本无法执行

**问题**: Permission denied

**解决方案**:
```bash
chmod +x scripts/quick-deploy.sh
chmod +x scripts/railway-deploy.sh
chmod +x scripts/verify-deployment.sh
```

### Node.js 未安装

**问题**: 脚本提示 Node.js 未安装

**解决方案**:
```bash
# 安装 Node.js
# Ubuntu/Debian
sudo apt-get install nodejs npm

# macOS
brew install node

# 或使用 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 22
```

### Git 未初始化

**问题**: 脚本提示不在 Git 仓库中

**解决方案**:
```bash
# 初始化 Git 仓库
cd /home/ubuntu/doc-similarity-analyzer
git init
git remote add origin https://github.com/leixingti/doc-similarity-analyzer.git
```

### 验证脚本无法连接

**问题**: verify-deployment.sh 无法连接到应用

**解决方案**:
1. 确保应用 URL 正确
2. 等待部署完成
3. 检查应用是否在线
4. 查看 Railway 日志

---

## 📚 相关文档

- [RAILWAY_QUICK_START.md](./RAILWAY_QUICK_START.md) - 快速部署指南
- [RAILWAY_SETUP_GUIDE.md](./RAILWAY_SETUP_GUIDE.md) - 详细部署指南
- [RAILWAY_DEPLOYMENT.md](./RAILWAY_DEPLOYMENT.md) - 完整参考文档
- [RAILWAY_DEPLOYMENT_READY.md](./RAILWAY_DEPLOYMENT_READY.md) - 部署检查清单

---

## ✅ 检查清单

### 部署前
- [ ] Node.js 已安装
- [ ] Git 已初始化
- [ ] 代码已推送到 GitHub
- [ ] 已运行快速部署脚本

### 部署中
- [ ] 在 Railway 创建了项目
- [ ] 添加了 MySQL 数据库
- [ ] 配置了环境变量
- [ ] 部署已开始

### 部署后
- [ ] 应用已部署成功
- [ ] 已运行验证脚本
- [ ] 所有检查都通过了
- [ ] 应用功能正常

---

## 🎉 部署成功！

如果所有步骤都完成，恭喜您！应用已成功部署到 Railway。

**下一步:**
1. 配置自定义域名（可选）
2. 设置监控和告警（可选）
3. 定期检查应用状态

---

**最后更新**: 2026年1月26日  
**版本**: 1.0.0
